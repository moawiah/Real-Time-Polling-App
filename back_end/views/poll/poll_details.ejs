
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Poll Details</title>
</head>
<body>

<%- include('../partials/navbar') %>

<div class="container">
    <div class="details content">
            <a class="single" href="/single-poll/<%= poll._id %>"><h3 class="title"><%= poll.question  %></h3>  </a>
            <% if (poll.options.length > 0) { %>
                <% poll.options.forEach(option => { %>
                    <p class="option"><%= option  %></p>
                    <% }) %>
                <% } %>
                    <!-- <a class="delete" poll_delete="<%= poll._id %>">Delete</a> -->
                    <!-- <a class="delete" href="/delete-poll/<%= poll._id %>">Delete</a> -->
                    
                    <form action="/delete-poll/<%= poll._id %>?_method=DELETE" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="delete" type="submit">
                            <img src="/trashcan.svg" alt="delete icon">
                        </button>
                      </form>
    </div>
</div>

</body>
</html>
